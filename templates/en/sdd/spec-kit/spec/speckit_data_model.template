# Data Model: [FEATURE NAME]

> Spec-Kit format (v0.10.x). Phase 1 OPTIONAL artifact — generate ONLY if the
> feature warrants it (non-trivial data semantics). Captures entities, fields,
> relationships, validation rules, and state transitions. Derive entities from
> spec.md's Key Entities. Lives at `specs/<feature-id>/data-model.md`.

## Entities

### [Entity Name]

| Field | Type | Notes |
|---|---|---|
| [field] | [type] | [constraints / description] |
| [field] | [type] | [constraints / description] |

**Validation Rules**:

- [Rule, e.g., "email MUST be unique and RFC-5322 valid"]
- [Rule]

**State Transitions** *(include only if the entity has a lifecycle)*:

- `[state A]` → `[state B]` when [trigger / condition]
- `[state B]` → `[state C]` when [trigger / condition]

### [Entity Name]

| Field | Type | Notes |
|---|---|---|
| [field] | [type] | [constraints / description] |

**Validation Rules**:

- [Rule]

## Relationships

- `[Entity A]` 1..* `[Entity B]` — [nature of the relationship]
- `[Entity C]` 1..1 `[Entity D]` — [nature of the relationship]
